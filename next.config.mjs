/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    
    basePath: '',
    images: {
        unoptimized: true,
        domains: ['goqpe.com'],
    },
      
      
};

export default nextConfig;
